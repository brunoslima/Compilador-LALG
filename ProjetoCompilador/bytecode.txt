program testebytecode;
int x, y, z, a, b, c, j, k;
boolean t;
begin
	
	a := 23 + 4;
	b := a * 10 + t;
	c := c >= 20;
	j := a + b - c div a;
	k := j and b;
	
	read(x,y);
	while x < y do
	begin
		write(x,y);
		x := x + 1
	end;

	z := x + y;
	if x >= 4 then write(z)
	else write(y);

	z := 10*x div 2;
	write(z)
	
end.