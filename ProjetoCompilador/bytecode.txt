program testebytecode;
int x, y, z, a, b, c;
begin
	
	a := 23 and 4;
	b := a * 10 > true;
	c := false;
	c := c + 20;
	
	read(x,y);
	while x < y do
	begin
		write(x,y);
		x := x + 1
	end;

	z := x + y;
	if x >= 4 then write(z)
	else write(y);

	z := 10*x div 2;
	write(z)
	
end.